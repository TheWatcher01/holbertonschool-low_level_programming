#include "search_algos.h"

/**
 * interpolation_search - Searches for a value in an array of integers
 * @array: Pointer to the first element of the array to search in
 * @size: Number of elements in array
 * @value: Value to search for
 *
 * This function performs an interpolation search on a sorted array of integers to find a
 * specific value. It returns the index of the occurrence of the value,
 * or -1 if the value is not found or the array is NULL.
 *
 * Return: The index where value is located, or -1 on failure
 */

int interpolation_search(int *array, size_t size, int value)
{
	
}